## API Report File for "@syncore/platform-node"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { MutationCtx } from '@syncore/core';
import { RecurringJobDefinition } from '@syncore/core';
import { RunScheduledJobsOptions } from '@syncore/core';
import { RunScheduledJobsResult } from '@syncore/core';
import { StorageObject } from '@syncore/core';
import { StorageWriteInput } from '@syncore/core';
import { SyncoreCapabilities } from '@syncore/core';
import { SyncoreClient } from '@syncore/core';
import { SyncoreDataModel } from '@syncore/core';
import { SyncoreFunctionRegistry } from '@syncore/core';
import { SyncoreResolvedComponents } from '@syncore/core';
import { SyncoreRuntime } from '@syncore/core';
import { SyncoreStorageAdapter } from '@syncore/core';

// @public
export function createTestSyncore<TSchema extends SyncoreDataModel>(options: CreateTestSyncoreOptions<TSchema>): Promise<TestSyncore<TSchema>>;

// @public
export interface CreateTestSyncoreOptions<TSchema extends SyncoreDataModel> {
    capabilities?: SyncoreCapabilities;
    components?: SyncoreResolvedComponents;
    functions: SyncoreFunctionRegistry;
    recurringJobs?: RecurringJobDefinition[];
    schema: TSchema;
}

// @public
export interface FinishScheduledJobsOptions {
    maxIterations?: number;
}

// @public
export class InMemoryStorageAdapter implements SyncoreStorageAdapter {
    // (undocumented)
    delete(id: string): Promise<void>;
    // (undocumented)
    get(id: string): Promise<StorageObject | null>;
    // (undocumented)
    list(): Promise<StorageObject[]>;
    // (undocumented)
    put(id: string, input: StorageWriteInput): Promise<StorageObject>;
    // (undocumented)
    read(id: string): Promise<Uint8Array | null>;
    // (undocumented)
    readRange(id: string, offset: number, length: number): Promise<Uint8Array | null>;
}

// @public
export interface TestSyncore<TSchema extends SyncoreDataModel> extends AsyncDisposable {
    action: SyncoreClient["action"];
    client: SyncoreClient;
    dispose(): Promise<void>;
    finishScheduledJobs(options?: FinishScheduledJobsOptions): Promise<RunScheduledJobsResult>;
    mutation: SyncoreClient["mutation"];
    query: SyncoreClient["query"];
    run<TResult>(callback: (ctx: MutationCtx<TSchema>) => Promise<TResult> | TResult): Promise<TResult>;
    runScheduledJobs(options?: RunScheduledJobsOptions): Promise<RunScheduledJobsResult>;
    runtime: SyncoreRuntime<TSchema>;
}

// (No @packageDocumentation comment for this package)

```
