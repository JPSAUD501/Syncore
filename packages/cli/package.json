{
  "name": "@syncore/cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "bin": {
    "syncorejs": "./dist/index.mjs"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs",
      "default": "./dist/index.mjs"
    }
  },
  "scripts": {
    "build": "npm run clean && tsdown --config tsdown.config.ts",
    "build:deps": "npm run build --workspace @syncore/core && npm run build --workspace @syncore/devtools-protocol && npm run build --workspace @syncore/schema && npm run build --workspace @syncore/platform-node",
    "clean": "node -e \"const fs=require('node:fs');for (const target of ['dist','node_modules/.unrun','node_modules/.vite','node_modules/.vite-temp']) { fs.rmSync(target,{recursive:true,force:true}); }\"",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "syncorejs:dev": "syncorejs dev",
    "syncorejs:codegen": "syncorejs codegen"
  },
  "dependencies": {
    "@syncore/core": "^0.1.0",
    "@syncore/devtools-protocol": "^0.1.0",
    "@syncore/platform-node": "^0.1.0",
    "adm-zip": "^0.5.16",
    "commander": "^13.1.0",
    "tsx": "^4.19.3",
    "ws": "^8.18.0"
  }
}
