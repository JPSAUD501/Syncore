{
  "name": "syncore-example-sveltekit",
  "private": true,
  "type": "module",
  "scripts": {
    "syncore:dev": "pnpm --dir ../../packages/cli exec tsx src/index.ts dev",
    "syncore:codegen": "pnpm --dir ../../packages/cli exec tsx src/index.ts codegen",
    "build:deps": "pnpm --filter syncore build:standalone",
    "dev": "pnpm run build:deps && pnpm run syncore:codegen && concurrently --kill-others-on-fail --names syncore,app --prefix-colors yellow,cyan \"pnpm run syncore:dev\" \"pnpm run dev:app\"",
    "dev:app": "vite dev",
    "build": "pnpm run build:deps && pnpm run syncore:codegen && vite build",
    "typecheck": "pnpm run syncore:codegen && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@syncore/platform-web": "workspace:*",
    "@syncore/svelte": "workspace:*",
    "sql.js": "^1.13.0",
    "svelte": "^5.0.0",
    "syncore": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "concurrently": "^9.1.2",
    "typescript": "^5.8.2",
    "vite": "^6.4.1"
  }
}
