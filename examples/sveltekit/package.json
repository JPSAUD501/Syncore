{
  "name": "syncore-example-sveltekit",
  "private": true,
  "type": "module",
  "scripts": {
    "syncore:dev": "bun run --cwd ../../packages/cli tsx src/index.ts dev",
    "syncore:codegen": "bun run --cwd ../../packages/cli tsx src/index.ts codegen",
    "build:deps": "bun run --filter syncore build:standalone",
    "dev": "bun run build:deps && bun run syncore:codegen && concurrently --kill-others-on-fail --names syncore,app --prefix-colors yellow,cyan \"bun run syncore:dev\" \"bun run dev:app\"",
    "dev:app": "vite dev",
    "build": "bun run build:deps && bun run syncore:codegen && vite build",
    "typecheck": "bun run syncore:codegen && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@syncore/platform-web": "workspace:*",
    "@syncore/svelte": "workspace:*",
    "sql.js": "^1.13.0",
    "svelte": "^5.0.0",
    "syncore": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "concurrently": "^9.1.2",
    "typescript": "^5.8.2",
    "vite": "^6.4.1"
  }
}
